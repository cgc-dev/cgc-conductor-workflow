# Write Plan

@CGC-Agents Write a detailed implementation plan for the spec at `docs/plans/<spec-file>.md`.

Include: step-by-step tasks, estimated effort, files to modify, dependencies between steps, and test strategy.
Save to `docs/plans/<feature-name>-plan.md`.
